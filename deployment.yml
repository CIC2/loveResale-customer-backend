apiVersion: apps/v1
kind: Deployment
metadata:
  name: resale-customer-backend
  namespace: dev-resale
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resale-customer-backend
  template:
    metadata:
      labels:
        app: resale-customer-backend
    spec:
      # <- add the imagePullSecrets here
      imagePullSecrets:
        - name: icr-secret
      containers:
        - name: resale-customer-backend
          image: de.icr.io/dev-registry/loveresale-customer-backend:latest
          args:
            - --spring.application.json=$(BINDING)
          env:
            - name: BINDING
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: application-properties
            - name: DB_CONNECTION
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_CONNECTION
            - name: DB_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: backend-properties
                  key: DB_PASSWORD
          ports:
            - containerPort: 8025
          resources:
            requests:
              cpu: 125m
              memory: 350Mi
---
apiVersion: v1
kind: Service
metadata:
  name: resale-customer-service-cip
  namespace: dev-resale
  labels:
    app: resale-customer-backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8025
  selector:
    app: resale-customer-backend
